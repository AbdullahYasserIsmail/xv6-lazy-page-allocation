# Lazy Memory Allocation in xv6

This is a simple project that adds **lazy allocation** to the xv6 RISC-V kernel.

## Features

- Modified `sbrk()`
- On-demand page allocation
- `lazytest` for validation

```sh
make qemu
